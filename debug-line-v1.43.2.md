
  "simple-widgets": "^1.43.2",

```js
        var Sr = (e, n) => {
        debug;
        let t = pn(n), r = Xn(t), c = Fo(n, e), g2 = r ? To(r, c, t) : "BIG PROBLEM: " + t + " has no keys defined in dbStruct.";
        return { gqlTable: t, where: g2 };
      };
```
